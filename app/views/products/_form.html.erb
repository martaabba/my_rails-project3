<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="name">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
  </div>
  
  <div class="field">
    <div class="description">
      <%= f.label :description %><br>
      <%= f.text_area :description %>
    </div>
  </div>
  
  <div class="field">
    <div class="image_field">
      <%= f.label :image_url %><br>
      <%= f.text_field :image_url %>
    </div>
  </div>
  
  <div class="field">
    <div class="color">
      <%= f.label :color %><br>
      <%= f.text_field :color %>
    </div>
  </div>
  
  <div class="field">
    <div class="price"></div>
      <%= f.label :price %><br>
      <%= f.text_field :price %>
    </div>
  </div>
  
  <div class="actions">
    <%= submit_tag("Save",class:"btn btn-default btn-green") %>
  </div>
  
<% end %>
